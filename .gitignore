# Environment / secrets
.env
.env.local

# macOS / OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.sublime-project
*.sublime-workspace

# Composer / Bedrock
/vendor/
/web/wp/

# Node
node_modules/
npm-debug.log*
yarn-error.log*

# Logs
*.log
debug.log

# Docker / database
db_data/
caddy_data/
caddy_config/

# Build output (theme / tooling)
build/
*.map

# Ignore dist except compiled theme assets (deployed via DeployHQ build)
dist/
!web/app/themes/ai-dev/dist/
!web/app/themes/ai-dev/dist/**

# Optional: keep lockfile in repo — remove ignore if you commit composer.lock
# composer.lock

# Premium Composer auth (do not commit)
auth.json

# Local SSL copies in theme (install script)
web/app/themes/ai-dev/local-ssl/

# Generated local CA (per machine; server.crt/key are committed for Binder-style dev)
.docker/ca.key
.docker/ca.srl
.docker/server.csr

# WordPress uploads (Bedrock: web/app/uploads)
web/app/uploads/

# Database dumps
*.sql
